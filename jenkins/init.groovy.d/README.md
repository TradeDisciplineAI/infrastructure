# Jenkins Initialization Groovy Scripts

This directory is a placeholder for future initialization Groovy scripts.

Any `.groovy` scripts placed in this directory (which mounts to `/var/jenkins_home/init.groovy.d/`) will be executed automatically by Jenkins on startup. These scripts are typically used to automate tasks like configuring credentials, configuring system properties, or creating default jobs.
