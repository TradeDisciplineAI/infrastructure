# Jenkins Configuration as Code (JCasC)

This directory is a placeholder for future JCasC YAML configuration files.

When JCasC is implemented, configuration files placed here can be loaded automatically by setting the `CASC_JENKINS_CONFIG` environment variable to point to this directory (e.g., `/var/jenkins_home/casc`) or mounting this directory to `/var/jenkins_home/casc`.